First: What Is a Linked List?

Unlike arrays:
- Arrays = contiguous memory
- Linked Lists = scattered memory + pointers

Each element is a node:
`[Data | Next] → [Data | Next] → [Data | null]`

It’s a treasure hunt where each box tells you where the next box is 🗺️